version: "3"
services:
  traefik:
    image: traefik:${VERSION.TRAEFIK}
    container_name: TRAEFIK
    env_file:
      - ../.env
      - .env-overwrite
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    networks:
      - traefik
    ports:
      - 80:80
      - 443:443
      # RabbitMQ MQTT
      - 1883:1883
    environment:
      - CF_API_EMAIL=${CLOUDFLAREMAIL.TRAEFIK}
      - CF_DNS_API_TOKEN=${CLOUDFLARETOKEN.TRAEFIK}
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./data/traefik.yml:/traefik.yml:ro
      - ./data/acme.json:/acme.json
      - ./data/config.yml:/config.yml:ro
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.${SERVICE.TRAEFIK}.entrypoints=http"
      - "traefik.http.routers.${SERVICE.TRAEFIK}.rule=Host(`${CONTAINERURL.TRAEFIK}`)"
      - "traefik.http.middlewares.${SERVICE.TRAEFIK}-auth.basicauth.users=${TRAEFIKUSER}:${TRAEFIKPASSWORD}"
      - "traefik.http.middlewares.${SERVICE.TRAEFIK}-https-redirect.redirectscheme.scheme=https"
      - "traefik.http.middlewares.sslheader.headers.customrequestheaders.X-Forwarded-Proto=https"
      - "traefik.http.routers.${SERVICE.TRAEFIK}.middlewares=traefik-https-redirect"
      - "traefik.http.routers.${SERVICE.TRAEFIK}-secure.entrypoints=https"
      - "traefik.http.routers.${SERVICE.TRAEFIK}-secure.rule=Host(`${CONTAINERURL.TRAEFIK}`)"
      - "traefik.http.routers.${SERVICE.TRAEFIK}-secure.middlewares=traefik-auth"
      - "traefik.http.routers.${SERVICE.TRAEFIK}-secure.tls=true"
      - "traefik.http.routers.${SERVICE.TRAEFIK}-secure.tls.certresolver=cloudflare"
      - "traefik.http.routers.${SERVICE.TRAEFIK}-secure.tls.domains[0].main=local.asuras.network"
      - "traefik.http.routers.${SERVICE.TRAEFIK}-secure.tls.domains[0].sans=*.local.asuras.network"
      - "traefik.http.routers.${SERVICE.TRAEFIK}-secure.service=api@internal"
      - "flame.type=application"
      - "flame.name=Traefik"
      - "flame.url=${CONTAINERURL.TRAEFIK}"
      - "flame.icon=traffic-light"

networks:
  traefik:
    external: true
